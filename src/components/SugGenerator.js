import React from 'react';

function SugGenerator({handleClick}) {
    return (
        <div>
        
            <h1>Activity Generator</h1>
            



            <button onClick={handleClick}>Generate</button>
            
            
            </div>
        
    );
}

export default SugGenerator;